#include "main.h"
		
		
/**
		
 * main - check the code.
 *
 * Return:  0
 * Return: else 1.
		
 */
		
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

